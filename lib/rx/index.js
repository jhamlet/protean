var Rx = require('rx');

// Module paths to import
[
].
    // require and execute the given function passing in the Rx object for
    // extension
    forEach(function (m) { require(m)(Rx); });

module.exports = Rx;
