var cp         = require('child_process');
var Rx         = require('rx');
var assign     = require('lodash/object/assign');
var Observable = Rx.Observable;

assign(exports, {
    sh: function sh (command) {
    }
});
