/*globals describe, it, before*/

require('should');

describe('Collection', function () {
    var Collection = require('protean/collection');
    var coll;

    before(function () { coll = new Collection(); });

    describe('#constructor(entries)', () => {
    });

});
